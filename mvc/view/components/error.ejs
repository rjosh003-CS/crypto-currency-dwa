<% if (typeof error !== 'undefined' && error) { %>

  <div class="error-message">
    <% if (error.status) { %>
        <b><%= error.status %> : </b>
    <%}%>

    <% if (error.message) { %>
        <%= error.message %>
    <%}%>
  </div>
  
<% } %>
